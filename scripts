1. 	useradd devops-ankit
	passwd devops-ankit
	usermod -aG nchl devops-ankit
4.	tar -xvf jdk-8u221-linux-x64.tar.gz
	tar -zvf apache-tomcat-8.5.20.tar.gz
5. 	firewall-cmd --permanent --add-port=80/tcp
	firewall-cmd --permanent --add-port=443/tcp
	firewall-cmd --permanent --add-port=53/tcp
7.	create table devops (id numeric, candidate_name varchar, address varchar);
	insert into devops (id, candidate_name, address) values (1, 'Ankit Neupane', 'Kathmandu');1
8. netstat -tupan
9. netstat -tupan

